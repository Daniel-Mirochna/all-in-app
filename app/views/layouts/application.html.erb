<!DOCTYPE html>
<html>
<head>
  <title>All in App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%#= javascript_importmap_tags %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>
<body class="dark">
    <%= render partial: "layouts/partials/navbar" %>
    <main class="container mx-auto mt-28 flex">
      <% flash.each do |type, msg| %>
        <div class="absolute z-10 top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 text-xl py-8 px-16 rounded-lg drop-shadow-[0_4px_15px_rgba(255,255,255,0.15)] <%= type == 'alert' ? 'bg-red-800' : 'bg-purple-800' %>">
          <i class="fa-solid fa-xmark absolute text-xl top-1 right-1 cursor-pointer" onclick="this.parentElement.outerHTML =''"></i>
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </main>
</body>
</html>
